# Base URL of the BizSocial360 API used by server components.
API_URL=http://localhost:4000

# Base URL of the API exposed to the browser (client components / OAuth links).
NEXT_PUBLIC_API_URL=http://localhost:4000
